{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Claude Desk main window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
